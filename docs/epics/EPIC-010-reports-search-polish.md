# EPIC-010: Reports, Search & Polish

status: ready-for-qa
environment: dev
phase: 4
priority: P2
area: Polish
retries: 0
prd: ../product/PRD.md
stories: ../product/USER-STORIES.md (US-POL-1 … US-POL-4)
tasks: ../product/ENGINEERING-TASKS.md (T-100 … T-103)

## Goal

Round out the system: digests that summarize instead of spam, the event settlement
report that closes a concert cleanly, permission-scoped global search, and mobile/PWA
polish for backstage use.

## User Stories

- **US-POL-1** — Opt-in daily digest + weekly executive digest.
- **US-POL-2** — Event settlement report, exportable.
- **US-POL-3** — Global search (⌘K) scoped by permissions.
- **US-POL-4** — Core flows usable on a phone (PWA).

## Tasks

### Digests

- [x] **T-100** node-cron + SMTP: opt-in daily digest (per-user content) + weekly executive digest for the Owner.

### Settlement report

- [x] **T-101** Per-event settlement report: budget vs actual by division, approval history, outstanding items; print/PDF export.

### Global search

- [x] **T-102** ⌘K search across tasks, events, files, people — every result filtered through the permission module.

### Mobile / PWA

- [x] **T-103** PWA manifest + responsive audit of core flows (My Tasks, task detail, approvals, run of show) + offline-tolerant shell.

## Acceptance Criteria

**Epic-level**

- Digests send on schedule, respect opt-ins, and contain only the recipient's visible data.
- Settlement totals reconcile with EPIC-005 records (test).
- Search never returns a record the user can't open (negative tests per role, incl. External).
- Core flows pass mobile viewport checks; app is installable.

**Per-task**

- **T-100** — schedule + content verified in dev transport.
- **T-101** — reconciliation test green; export clean.
- **T-102** — role-scoped search tests green.
- **T-103** — Lighthouse PWA installability passes; viewport audit documented.

> Each task also satisfies `../DEFINITION-OF-DONE.md`.

## Automation Log

- 2026-08-06 **T-103 — PWA + responsive audit** (`406efea`). Manifest at
  `src/app/manifest.ts` (standalone, portrait, monochrome) with 192/512/maskable
  icons generated by `scripts/generate-icons.mjs` (5x7 bitmap of the RVC lockup,
  zlib only — no image dependency added). `viewport` export in the root layout
  sets `viewport-fit=cover` + per-scheme theme colour. Offline shell
  `public/sw.js` is deliberately narrow: it **never** caches HTML or `/api`
  responses because backstage phones are shared between crew — only
  build-immutable assets and `/offline`; navigations are network-first.
  **Verified in a real browser** (headless Chromium): worker activates at scope
  `/`, cache holds only static chunks + `/offline`, an offline navigation
  renders the fallback, `/api/search` is not cached.
  **Viewport audit** — iPhone 12 emulation plus a strict 390px context (which
  disables Chrome's shrink-to-fit, so real overflow is measurable) across
  my-tasks, task detail, approvals, dashboard, settings, calendar, timeline,
  events, event workspace, run-of-show, board, budget, documents, gantt,
  offline. **One real failure found:** the dashboard laid out at 479px on a
  390px screen. Root cause was *not* a row's content but the
  milestones/blockers `grid` — grid items default to `min-width:auto`, so the
  column was sized by its widest row rather than the viewport. Fixed with
  `min-w-0` on both grid children plus `min-w-0` on `EventChip` (a long event
  name now truncates instead of forcing its row wide). All 15 pages re-measure
  at exactly 390px with no zoom-out. Installability was confirmed by criteria
  (manifest linked + 192/512/maskable icons + `display: standalone` +
  `start_url` + active SW + theme-color + viewport), not by Lighthouse —
  Lighthouse is not in the toolchain and headless Chromium suppresses
  `beforeinstallprompt`. **Re-run the audit before the next release** if the
  dashboard or any shared list row changes.

- 2026-08-06 **T-100 · T-101 · T-102** (`70ef915`). Digests: two opt-in booleans
  on `profiles` (both default OFF) + self-service `/settings`; node-cron sends
  the personal digest 07:00 WIB daily and the executive digest Mondays 07:00
  WIB, **timezone pinned** so a UTC container never shifts the send hour.
  Content is built from the same permission-scoped services the pages use, and
  the weekly digest is double-gated (role check in the sender +
  `assertCan(dashboard.view)` inside `getPortfolio`) so flipping the flag can
  never surface portfolio data to a member. Settlement report (T-101) is the
  financial close-out, distinct from the progress report: budget vs actual by
  division, approval history, outstanding items, Owner/Admin only, downloads
  audited; reconciliation math is isolated in `settlement-math.ts` and
  unit-tested. Search (T-102) scoping is a pure, unit-tested function
  (`visibleDivisionsFor` / `canSearch`) — externals refused outright, members
  limited to their divisions.
  **Migration chain repair (important):** `0019` had been hand-written with no
  drizzle snapshot, and the `0017`/`0018` snapshots were never regenerated, so
  the chain had been stale since run-of-show — any `db:generate` would have
  re-emitted already-applied tables. Regenerated `0019_snapshot.json` from the
  live schema (drizzle now reports "no schema changes") and committed the
  missing `0018` snapshot. The `0019` DDL uses `IF NOT EXISTS` because the
  columns had already been applied to the dev database out-of-band, outside
  `__drizzle_migrations`. **Lesson: never hand-write a migration — always
  `pnpm db:generate --name <slug>`, or the snapshot chain silently rots.**
  `src/db/schema/org.ts` is a protected path; the two columns are required by
  T-100's opt-in acceptance criterion.

- 2026-08-06 **Early delivery (Owner request): Event Progress Report PDF** — `/api/events/[id]/report` (owner/admin, dashboard.view gate; downloads logged to audit). Data in `src/lib/reports/service.ts` (event/phase/health, task progress per division + overdue table + checklist rollup, budget rollup per line, approvals incl. pending items, handoffs, guest invites+submissions, run-of-show span); renderer `src/lib/reports/pdf.ts` (pdfkit, monochrome A4, auto-paginating tables; `serverExternalPackages: ["pdfkit"]` so standalone tracing ships the AFM fonts). Button "Progress report (PDF)" on the event workspace. Verified live: owner downloads valid PDF, staff gets 403. Distinct from T-101 settlement report (financial close-out) — that remains open.

- 2026-08-06 Epic created by `/agentic-init` from PLAN §9 Phase 4 — pending kickoff.

## Dependencies

- EPIC-005/006 (settlement + email base), EPIC-008 (documents in search). Last epic of v1.
